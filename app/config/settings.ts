export const EXPIRATION_MODAL_SETTINGS = {
  // Time in milliseconds before showing the expiration modal
  SHOW_AFTER_MS: 60000 * 10, // 10 minute
  
  // Other potential settings for the expiration modal
  // AUTO_HIDE_AFTER_MS: 30000, // Auto hide after 30 seconds if needed
} 